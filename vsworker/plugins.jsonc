{
  // Plugins bundled into VsWorker builds. Edit this file, then run:
  //   bun run --cwd vsworker plugins generate
  // See vsworker/README.md for the field reference and the constraints a bundled plugin must satisfy.
  "$schema": "./plugins.schema.json",
  "plugins": [
    {
      "id": "hello",
      "source": "local",
      "defaultEnabled": false,
      "description": "Template and smoke test for the bundling pipeline. Off by default.",
    },
  ],
}
