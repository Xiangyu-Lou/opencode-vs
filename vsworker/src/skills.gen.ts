// GENERATED by `bun run --cwd vsworker bundle generate` — edit vsworker/bundle.jsonc instead.
// Skill files are inlined rather than imported as assets so the same module works under `bun dev`, inside the
// compiled binary, and inside the Node desktop bundle. The runtime writes them to the cache dir on first use.
import type { Raw } from "./skills"

export const hash = "5e5ba0b747a2e15a412a8da723c53ea1abae0c34616cd025a2a9ac9110158b5d"

export const skills: readonly Raw[] = [
  {
    id: "hello",
    defaultEnabled: true,
    description: "Template and smoke test for skill bundling.",
    files: [
      {
        path: "SKILL.md",
        encoding: "utf8",
        executable: false,
        data: "---\nname: hello\ndescription: Template and smoke test for the VsWorker skill bundling pipeline. Use only when someone explicitly asks to verify that bundled skills reach the model.\n---\n\n# Bundled skill template\n\nThis skill ships inside every VsWorker build and is off by default, so a release is unaffected until someone\nturns it on with `opencode vsworker skills enable hello`. It exists to prove the pipeline end to end: the file\nyou are reading was compiled into the binary, written to the cache directory at startup, and handed to you by\nthe `skill` tool.\n\nCopy `vsworker/skills/hello/` as the starting point for a real in-house skill, or run\n`bun run --cwd vsworker bundle import skill <name>` to vendor one you already keep in your global skills\ndirectory. Read `vsworker/README.md` for the field reference.\n\n## What to do when this skill loads\n\nSay that the bundled skill pipeline works, and report the base directory printed above the file list. Nothing\nelse. This skill carries no instructions for real work.\n",
      },
    ],
  },
  {
    id: "well-intervention-recommendation",
    defaultEnabled: true,
    description: "井下作业措施推荐：问答对 → 图谱 两级检索。平台地址与阈值放在该 skill 的 env.json 里。",
    files: [
      {
        path: "SKILL.md",
        encoding: "utf8",
        executable: false,
        data: '---\nname: well-intervention-recommendation\ndescription: "井下作业出现事故或复杂情况（卡钻、井漏、落物、射孔失败、坐封失败、HSE 事件等）时推荐处理措施。先检索尼日尔项目历史处置记录（问答对），未命中再查《钻井事故与复杂问题》（也就是知识库或知识图谱）；历史记录优先于《钻井事故与复杂问题》。"\nlicense: Proprietary\n---\n\n# 井下作业措施推荐\n\n用户描述一个井下事故或复杂情况，本 skill 给出处理措施。\n\n**一条硬规则贯穿全文：历史处置记录优先于《钻井事故与复杂问题》。** 尼日尔项目 2008–2026 的 148 条真实处置记录\n放在平台的问答对里；《钻井事故与复杂问题》（蒋希文, 2006）放在图谱/知识库里。\n**两者冲突时以历史记录为准，不得用《钻井事故与复杂问题》"纠正"历史做法。**\n（实例：《钻井事故与复杂问题》给的卡钻顺序是 强力活动 → 震击 → 解卡剂；尼日尔 9 起卡钻的实测是震击单独使用 0/4 有效、\n浸泡解卡剂 2/2 有效。问「先震击还是先泡解卡剂」必须答解卡剂。）\n\n**当前流程：问答对 → 图谱**，命中即退出。\n兜底级那一格是**知识库或图谱、二选一**（`env.json` 的 `KB_ENABLED` / `GRAPH_ENABLED`，\n两个都开脚本直接报配置错误，也可以都关）—— 现在放的是图谱。\n两者装的都是同一本《钻井事故与复杂问题》，只是切法不同：知识库返回**原文片段**，图谱返回**生成的答案**。\n2026-09-10 用 20 条题实测选的图谱：15 条技术题图谱 15 ✓、知识库只有 3 ✓，\n且知识库有 4 条**高分假命中**（问「起钻抽吸怎么预防」返回「泥包的预防」，0.7798）。\n代价是慢：图谱平均 27 秒、知识库 0.6 秒。对比见仓库 `docs/兜底级对比.md`。\n\n**每个回答都必须说清依据来自哪一层**：尼日尔历史记录（问答对），还是《钻井事故与复杂问题》（知识库/图谱）。\n知识库里的《钻井事故与复杂问题》含书上的「例X」现场实例（别的项目的井）\n\n当前部署的三件事，看输出时要知道：\n- **五个类别在同一个问答库里**，所以「聚合条目 / 单案例」是按答案正文判的\n  （`【结论】` 开头 = 聚合，`【井号】`／`【队号】` 开头 = 单案例），输出里直接标出来。\n- **兜底级现在是图谱**（`env.json` 里 `GRAPH_ENABLED=1`、`KB_ENABLED=0`；两个开关都写出来，不能同时为 1）。\n  图谱库和知识库装的都是《钻井事故与复杂问题》，台账已经从两个库里移除，\n  所以**兜底级命中一律来自《钻井事故与复杂问题》，【依据】就写这五个字的书名**\n  —— 不写「图谱」「知识库」，也不补「台账里没有同类记录」。\n  图谱一条要 20～48 秒（知识库是 0.6 秒），这是正常的，别当成卡死。\n- **图谱答不上来时会用一段通顺的中文硬凑**（先说「描述较少 / 无法直接回答」，\n  再拿邻近话题充数）。`is_graph_refusal()` 已按措辞把这类判成落空 → `no_answer`，\n  所以**看到 `decision=no_answer` 就照实说没有，不要自己去把图谱那段话捞回来用**。\n- **定义题会被挡在问答对之外**：「什么是X」「X是什么意思」这类问题，阈值临时提到\n  `QA_DEFINITION_THRESHOLD`（0.90）—— 台账记录的是事故处置，不记术语定义，定义应由《钻井事故与复杂问题》回答。\n  输出里会打一行 `[提问类型] 定义题`。看到它就知道为什么没走问答对。\n\n---\n\n## 工作流程\n\n### 第 1 步：把用户这句话改写成能独立看懂的检索句\n\n**多轮对话里这一步是必做的，不是可选的。** 检索是纯向量相似度比整句话，\n用户第二轮的「那泡多久？」缺主语，原样检索必然跑偏。\n\n- 补全：事故/事件类型、井号或队号、年月 —— **只补上文明确出现过的**，不要猜参数。\n- 用户问「这类事故一般怎么办」→ **不要带井名**；问「这口井那次」→ **带井名**。\n- 换了事故类型就当新问题，丢掉上一轮的井号。\n- 追问的是上一条答案里**已经写着**的内容（为什么不先震击、损失多少工时）→ 直接看上一条答案，**不要重新检索**。\n\n规则全文与 7 个改写示例：`references/查询改写.md`。改写幅度大时，回答开头用一行交代实际检索的是什么。\n\n### 第 2 步：跑检索（只有这一条命令）\n\n```bash\npython3 scripts/recommend.py -q "改写后的自洽问题"\n```\n\n脚本内部就是「问答对 → 未命中才兜底级」，**命中即退出**，不需要你分两次调。\n\n可选参数：`--threshold 0.72`（临时改问答对阈值）、`--k 10`（多召回几条）、\n`--kb-threshold 0.5`（临时改知识库阈值）、`--json`（要结构化结果时）。\n\n**用户点名要查某一层时，用 `--source` 跳过默认顺序：**\n\n| 用户说 | 命令 |\n|---|---|\n| 「书上怎么说」「教材是怎么写的」「查《钻井事故与复杂问题》」「不要历史记录，看通用做法」 | `--source kb` |\n| 「查图谱」「按知识图谱回答」 | `--source graph` |\n| 「只看我们自己的记录」「不要书上的」 | `--source qa` |\n| 没提来源（绝大多数情况） | 不加，走默认的 `auto` |\n\n`--source kb/graph` 会**跳过问答对**，等于放弃「历史优先」，所以**只在用户明确要求时用**，\n不要因为自己觉得台账里可能没有就先跳过 —— 那是 `auto` 该做的判断。\n指定的库在 `env.json` 里是关着的（比如现在的图谱）也照查，输出会打一行 `[提示]` 说明结果可能不可靠。\n\n### 第 3 步：读输出第一行的 `[决策]`，照表动作\n\n| `[RESULT] decision=` | 含义 | 你要做的事 |\n|---|---|---|\n| `use_qa_answer` | 问答对命中 | **把「答案正文」原样输出**给用户。不要改写、不要压缩、不要再补《钻井事故与复杂问题》的内容、不要加追问示例。答案是按交付措辞写好的。 |\n| `use_kb_answer` | 问答对未命中，知识库有片段 | 片段是**《钻井事故与复杂问题》的原文，不是写好的答案**：按下面的「统一回答结构」自己归纳成结论 + 步骤，**必须写明**「依据来自《钻井事故与复杂问题》」。不要把片段整段倒给用户。 |\n| `use_graph_answer` | 问答对未命中，图谱有答案 | 照图谱答案回答，但**必须写明**「依据来自《钻井事故与复杂问题》」。 |\n| `no_answer` | 没有可用答案（两级都没命中，或兜底级被关着） | **不要编造措施。** 说明历史台账里没有同类记录（兜底级关着就说明那一级暂时不可用），请用户补充井号/井深/工况，或转人工。**但 `kb=empty` 且问题是长句/对比句时先重试一次**：把问题压成术语再查，见下。 |\n| `route_to_well_control` | 命中井控红线（井喷等） | 不做措施推荐。回答：立即执行井控程序（按设计关井、压井）、上报值班、转人工。检索结果只能当背景，不得当作本项目处置经验。 |\n\n用 `--source kb/graph` 跳过问答对时，输出会打 `[跳过问答对]`：这时**必须**告诉用户\n「本次只查了《钻井事故与复杂问题》，没查尼日尔历史记录」，并说明默认流程是先查历史记录。\n\n输出里还会出现的提示行：\n\n- `[⚠️ 问答对未发布]` —— 召回到了但 `answer` 是 null：平台上该类别没点「发布」。\n  **告诉用户去发布**，不要假装没看见（参见 `平台部署说明.md` §8.4）。\n- `[排序调整]` —— 问题没带井名，已把「综合结论」聚合条目提为首选。正常现象。\n- `[同时命中的其他条目]` —— 供你判断用，用户没追问就别拼进答案。\n\n---\n\n## 统一回答结构（三种来源都用这一套）\n\n不管答案来自问答对、知识库还是图谱，给用户看到的结构应当一致。\n**三级检索是内部的事，用户看到的出处只有两种。**\n\n```\n【结论】一句话说清现在该做什么（最关键的动作放第一句）\n【依据】二选一，照抄，不加别的：\n        · 尼日尔历史记录（点名井号与年份）\n        · 《钻井事故与复杂问题》（知道章节就带上「第X章第Y节」）\n【处置步骤】1. 2. 3.（有先后就按先后写，别只给一堆并列选项）\n【注意】禁忌、升级/停止判据、缺失的信息\n```\n\n三条硬规则：\n\n1. **问答对命中时原样输出，不要再套模板** —— 它本来就是这个结构（`【结论】`/\n   `【尼日尔历史记录统计】`/`【依据记录】`/`【《钻井事故与复杂问题》】`），重排只会弄丢信息。\n2. **`【依据】` 只有两个取值**：`尼日尔历史记录` 或 `《钻井事故与复杂问题》`。两件事都不要做 ——\n   - **不写检索通道。**「问答对」「知识库」「图谱」「知识图谱生成答案」是内部三级检索的名字，\n     只出现在脚本输出的 `[来源]` 行里（`[来源] 图谱库（《钻井事故与复杂问题》）`），**不许进用户看到的答案**。\n     用户要知道的是「这话是台账里的还是书上的」，不是我们用哪个库查到的。\n     ——「《钻井事故与复杂问题》（知识图谱生成答案）」是错的，写「《钻井事故与复杂问题》」。\n   - **不写否定句。** 不补「尼日尔历史台账中没有同类记录」「是通用做法、非本项目实践」。\n     出处写清楚了，读的人自然知道不是台账。（`no_answer` 是另一回事：那时「没有同类记录」本身就是答案。）\n\n   片段里出现的井名、井深、事故经过是**书上别的项目的案例**，可以引用，但要说清那是《钻井事故与复杂问题》里的例子。\n3. 不编数字：井号、井深、工时、参数缺失就写「未记录」，不要从《钻井事故与复杂问题》的片段里挪一个数字过来充数。\n\n这是**措辞引导，不是格式校验**：某一节实在没内容就省掉，不要为了凑格式硬写。\n\n## 回答措辞（问答对答案正文已经按这套写好，你别破坏它）\n\n- 说「**尼日尔历史记录中**」，不说「本项目」。\n- 引用历史做法时点名**哪口井、哪一年**。\n- 来自书里的内容**必须显式写明**「依据来自《钻井事故与复杂问题》」。\n- **【依据】只有两种出处**：`尼日尔历史记录` / `《钻井事故与复杂问题》`。\n  不写「问答对」「知识库」「图谱」这些内部通道名，也不写「不是来自哪里」。\n- **不编造**井号、井深、时间、工时、参数 —— 缺失就写「未记录」。\n- 末尾不要加追问示例。\n\n---\n\n## 其他命令（排查和调优时才用）\n\n```bash\n# 配置与连通性自检：换环境、换 token、报鉴权错时先跑这个。看最后一行 [自检结果]\npython3 scripts/check_env.py\n\n# 只跑第一级，看召回明细（含阈值以下的）\npython3 scripts/qa_search.py -q "卡钻怎么处理？" --k 10 --show-all\n\n# 只跑兜底级：知识库片段（--full 看全文，默认每段截到 600 字）\npython3 scripts/kb_search.py -q "键槽卡钻怎么判断" --full\n\n# 只跑兜底级：图谱实体与关系（GRAPH_ENABLED=1 时才有意义）\npython3 scripts/graph_search.py -q "什么是键槽卡钻" --graph\n\n# 标定阈值：用 31 条正/负例扫一遍，最后一行给建议值，填回 env.json 的 QA_THRESHOLD\npython3 scripts/tune_threshold.py --cases assets/threshold-cases.txt --out /workspace/tune.json\n\n# 导入核对：从 dist 的问答对文件抽问法回查，确认整批真的进了向量库（每次重新导入后跑）\npython3 scripts/verify_import.py --sample 60          # --sample 0 = 全查，1353 条约 3.5 分钟\n```\n\n**所有配置都在 skill 根目录的 `env.json`**（平台注册 skill 时会自动解析它、把键值写进环境变量；\n本地跑脚本自己读同一份）。`_` 开头的键是注释。临时改一个值用同名环境变量覆盖，\n例如 `KB_ENABLED=0 python3 scripts/recommend.py -q "…"`；换一整份配置用 `WIR_ENV_FILE=/path/to/x.json`。\n\n脚本路径写**相对**（相对 skill 根），产物路径写**绝对**（`/workspace/...`）；不要 `cd` 到 skill 目录。\n若运行时的 cwd 不是 skill 根（例如在 Claude Code 里从仓库根执行），把命令前缀换成\n`python3 skills/well-intervention-recommendation/scripts/...`，其余参数不变。\n\n---\n\n## 故障对照表\n\n| 现象 | 原因 | 处理 |\n|---|---|---|\n| `[配置错误] PLATFORM_BASE_URL 没填` 等 | `env.json` 缺键或写错 | 补上那个键（所有变量都在 skill 根目录的 `env.json`），再跑 `check_env.py` |\n| 任何一级报 `status=610` / `611` | **多半是路由拼错，不是鉴权**。网关对不存在的路由一律回 610「No authentication information .」，带上格式合法的 `Authorization` 就变 611 —— 极像 token 过期。三级当前都不需要任何鉴权头 | 先核对 `platform_client.py` 顶部三个路径常量（图谱是 `/backend/lightrag/search`，**不带 `-py`**；问答对和知识库那两个才带）。路由没错，才考虑平台改回要鉴权，把 `PLATFORM_TOKEN` + `PLATFORM_EXTRA_HEADERS` 填进 `env.json` |\n| 连不上平台 | 不在内网 | 确认 VPN，再核对 `PLATFORM_BASE_URL` |\n| 请求返回 502 / 超时，但浏览器能打开平台 | 本机代理（Clash 等）把内网地址劫走了 | 脚本默认已绕开系统代理；若被人改成 `PLATFORM_USE_PROXY=1`，设回 `0` |\n| 召回到了但 `answer` 为 null | 问答对没发布 | 平台上五个类别逐个点「发布」，「未发布: N」归零 |\n| 图谱没收到 `[DONE]` | 超时或连接断 | 调大 `env.json` 的 `GRAPH_TIMEOUT`，重试一次 |\n| `[图谱错误] HTTP 404` | 路由不对。图谱是 `/backend/lightrag/search`，**不带 `-py`** | 核对 `platform_client.py` 顶部的 `GRAPH_PATH`。历史上写成 `lightrag-py` 时不是 404 而是 610，见上一行 |\n| 该命中的问题落到了兜底级 | 问答对阈值偏高 | 跑 `tune_threshold.py` 重新标定 |\n| `[配置错误] KB_ENABLED 和 GRAPH_ENABLED 只能开一个` | 兜底级是二选一 | `env.json` 里把其中一个设为 0（图谱入库期间留知识库） |\n| `[知识库] 零片段`，但问题明显是《钻井事故与复杂问题》该答的 | 知识库对长句/对比句极不友好（「司钻法和工程师法压井有什么区别」0 条，「压井方法」0.9902） | 把问题**压成术语**再查一次：`--source kb -q "压井方法"`。规则见 `references/查询改写.md` |\n| `[知识库] 零片段`，问题本身就离题 | 库里确实没有 | 如实答「没有同类记录」。**别调低 `KB_THRESHOLD`**：离题片段分值也有 0.12–0.25，压低等于放垃圾进来 |\n| 明明台账里有，却答了《钻井事故与复杂问题》 | 自己加了 `--source kb/graph` | 那两个只在用户点名要查《钻井事故与复杂问题》时用；默认永远是 `auto`（先查历史记录） |\n| 定义题被答成了措施推荐 | 定义题规则没拦住 | 看 `platform_client.is_definition()` 的匹配模式，补一条 |\n| 新加的问法检索不到 | 被写进了扩展问列 | 平台只向量化 `name` 列：每个问法必须占**独立一行**（`build_qa.py` 已如此，别改回 1+5 打包） |\n| 通用问题被某口井的单案例抢了命中 | 聚合条目扩展问不够 | 确认 `QA_PREFER_AGGREGATE=1`；仍不对就回仓库给聚合条目加扩展问 |\n\n## 不做的事\n\n- 不自己训练、不自己存问答对和《钻井事故与复杂问题》原文——检索全部走平台接口。\n- **兜底级只开一个**：知识库和图谱装的都是《钻井事故与复杂问题》，同时开只会让答案来源说不清。\n- **井喷不做措施推荐**：历史台账对井喷零覆盖，这里「历史优先」规则是反的，一律转井控程序与人工。\n- 不改写问答对返回的答案正文。\n',
      },
      {
        path: "env.json",
        encoding: "utf8",
        executable: false,
        data: '{\n  "PLATFORM_BASE_URL": "http://10.68.199.207",\n  "QA_ID_LIST": "3d457676e1c9472bbd2594d51e5c616f",\n  "QA_THRESHOLD": "0.69",\n  "QA_TOP_K": "10",\n  "KB_ID_LIST": "9d2acac7bbc344ca9c71d41173086270",\n  "GRAPH_ENABLED": "1",\n  "KB_ENABLED": "0",\n  "GRAPH_KB_ID": "c_ff97f2d1be32438caf315555c0f1693b"\n}\n',
      },
      {
        path: "references/查询改写.md",
        encoding: "utf8",
        executable: false,
        data: '# 多轮对话的检索语句改写\n\n## 为什么必须改写（2026-09-09 实测数据）\n\n| 用户原话 | 直接拿去检索 | 改写后 |\n|---|---|---|\n| 那泡多久 | **0.6090**，低于阈值 → 没答案 | 「卡钻浸泡解卡剂要泡多长时间」→ **0.8668** 命中 |\n| 损失了多少工时 | **0.6480**，而且首位是 **Fana-1**（跟上文那口井无关） | 「Koulele CE-19 2021年10月卡钻损失了多少工时」→ **0.8638**，命中的正是那口井 |\n\n第二行是最危险的情况：不改写不只是查不到，而是**查到别的井头上去**。\n\n## 为什么必须改写\n\n问答对这一级是**纯向量相似度**，比的是一整句话。用户第二轮的话通常省略主语宾语\n（「那泡多久？」「损失了多少？」），原样拿去检索，相似度会落到毫无关系的条目上，\n或者整体低于阈值白白掉到兜底级。**知识库/图谱那一级同样是单句检索，没有会话记忆。**\n\n所以：**每一轮都要先把用户这句话补成一个能独立看懂的问题，再去检索。**\n`scripts/recommend.py` 不做改写，传什么就查什么。\n\n## 七条规则\n\n1. **补全三件事**：事故/事件类型、井号或队号、时间（年月）。上文里有就补，没有就不补。\n2. **只补上文明确出现过的信息**。不要替用户猜井深、密度、工序——台账讲究「缺失就写未记录」，\n   检索语句里凭空加参数会把相似度带偏。\n3. **井名带不带，决定命中哪一层**：\n   - 用户问「这口井当时怎么处理的」→ **带井名**（命中单案例条目）。\n   - 用户问「这类事故一般怎么办」→ **不要带井名**（命中综合结论聚合条目）。\n     带了井名会被 148 条单案例抢命中。\n4. **HSE / 井场建设没有井号**，单案例主问题是「队号 + 年月」开头（例 `GW216 2009年11月交通事故`）。\n   同一队有多起同名事故（GW216/GW227/GW48 各有两起手指受伤），**必须带年月**才不会撞。\n5. **换了事故类型就当新问题**，把上一轮的井号和类型全部丢掉，别继承。\n6. **一次只查一个事故类型**。用户一句话里问了两件事，拆成两次 `recommend.py` 调用。\n7. **口语词保留 + 补规范词**，不要替换掉用户的原话。\n   例：「油管提不动」→ 「油管悬挂器粘连、提不动怎么处理」。\n\n## 知识库那一级要把问题压成术语（2026-09-09 实测）\n\n问答对和知识库对问句形态的偏好**相反**：问答对喜欢完整口语句（库里存的就是一条条问法），\n知识库（《钻井事故与复杂问题》的切片）对长句、对比句极不友好 —— 同一个意思换个说法，分值能从 0 跳到 0.99：\n\n| 检索句 | 知识库最高分 |\n|---|---|\n| 司钻法和工程师法压井有什么区别 | **0 条**（全都低于 0.6 阈值） |\n| 司钻法压井 | 0.9067 |\n| 压井方法 | 0.9902 |\n| 一次循环法压井步骤 | 0.9731 |\n\n所以看到 `decision=no_answer` 且 `kb=empty`、而问题本身又长又像对比题时，\n**别直接下结论说《钻井事故与复杂问题》里没有**，把问题压成术语再查一次：\n\n```bash\npython3 scripts/recommend.py -q "压井方法" --source kb\n```\n\n压法：去掉「有什么区别」「怎么办」「是不是」这类问式外壳，留**术语 + 动作**\n（「司钻法和工程师法压井有什么区别」→「压井方法」；「泡解卡剂要泡多久合适呢」→「浸泡解卡剂」）。\n一次不行就换个术语，**别去调 `KB_THRESHOLD`** —— 离题片段也有 0.12–0.25，压低阈值等于放垃圾进来。\n\n## 什么时候**不要**重新检索\n\n追问是在问**上一条答案里已有的内容**（「为什么不先震击」「泡多长时间」「损失了多少工时」），\n先在上一轮拿到的答案正文里找。找到了就直接答，**不要再检索**——\n重复检索只会拿回同一条答案，白花一次往返。\n只有答案里确实没有时，才按上面的规则改写并再查一次。\n\n## 改写示例\n\n| 轮次 | 用户原话 | 拿去检索的句子 | 说明 |\n|---|---|---|---|\n| 1 | 卡钻怎么处理 | `卡钻怎么处理` | 本来就自洽 |\n| 2 | 那要泡多久 | （先看上一条答案里有没有浸泡时间；没有才查）`卡钻浸泡解卡剂要泡多长时间` | 补主语 |\n| 3 | Koulele CE-19 那次呢 | `Koulele CE-19 2021年10月卡钻是怎么处理的？` | 带井名 → 单案例 |\n| 4 | 损失了多少工时 | （先看上一条答案的【损失工时】）`Koulele CE-19 2021年10月卡钻损失了多少工时` | 继承井名与类型 |\n| 5 | 井漏呢 | `井漏怎么处理` | 换类型：丢掉井名，不带 Koulele |\n| 6 | GW216 那次手指受伤 | `GW216 2023年6月手指受伤是怎么处理的？` | HSE 必须带年月 |\n| 7 | 先震击还是先泡解卡剂 | `卡钻该先震击还是先泡解卡剂？` | 冲突检验，必须走问答对 |\n\n## 改写后的一句交代\n\n改写幅度较大时（补了井号、年月、事故类型），在回答开头用一行告诉用户实际查的是什么，例如：\n\n> 按「Koulele CE-19 2021年10月卡钻」检索历史记录：\n\n这样用户能立刻发现补错了对象，而不是读完一段错答案才发现。\n',
      },
      {
        path: "scripts/check_env.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""配置与连通性自检。换环境、换 token、报鉴权错时先跑这个。\n\n  python3 scripts/check_env.py\n  python3 scripts/check_env.py --skip-graph -q "卡钻怎么处理？"\n\n最后一行一定是 [自检结果] PASS/FAIL，照它判断，别看退出码。\n"""\nfrom __future__ import annotations\n\nimport argparse\nimport sys\nimport urllib.error\nimport urllib.request\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parent))\n\nfrom platform_client import (\n    FALLBACK_CN,\n    ConfigError,\n    PlatformError,\n    fmt_sim,\n    graph_search,\n    kb_search,\n    load_config,\n    opener,\n    qa_search,\n    use_utf8,\n)\n\n\ndef show_config(cfg):\n    print("────── 当前配置 ──────")\n    print(f"  env.json 文件          {cfg.env_file}")\n    print(f"  PLATFORM_BASE_URL  {cfg.base_url or \'(空)\'}")\n    print(f"  PLATFORM_TOKEN     {cfg.masked_token()}")\n    if cfg.token:\n        print(f"  鉴权头             {cfg.auth_header}: {(cfg.auth_prefix + \' \') if cfg.auth_prefix else \'\'}<token>")\n    else:\n        print("  鉴权头             不发（问答对/知识库/图谱三级当前都不需要鉴权；若网关回 610 先查路由拼写，不是凭证问题）")\n    print(f"  网络               {\'走系统代理\' if cfg.use_proxy else \'直连（绕开系统代理）\'}")\n    if cfg.cookie:\n        print(f"  PLATFORM_COOKIE    已设置（{len(cfg.cookie)} 字符）")\n    if cfg.extra_headers:\n        print(f"  额外请求头         {\', \'.join(cfg.extra_headers)}")\n    if cfg.qa_type_id:\n        print(f"  QA_TYPE_ID         {cfg.qa_type_id}（单库检索）")\n    print(f"  QA_ID_LIST         {len(cfg.qa_ids)} 个问答库")\n    for qa_id in cfg.qa_ids:\n        print(f"      - {qa_id}  →  {cfg.qa_labels.get(qa_id, \'(未标注类别)\')}")\n    print(f"  QA_THRESHOLD       {cfg.qa_threshold}    QA_TOP_K {cfg.qa_top_k}")\n    print(f"  聚合优先           {\'开\' if cfg.prefer_aggregate else \'关\'}（margin {cfg.aggregate_margin}）")\n    print(f"  兜底级             {FALLBACK_CN[cfg.fallback]}"\n          + ("（知识库与图谱只能开一个）" if cfg.fallback != "none" else ""))\n    print(f"  KB_ID_LIST         {\'、\'.join(cfg.kb_ids) or \'(空)\'}    "\n          f"阈值 {cfg.kb_threshold}  top_k {cfg.kb_top_k}  expand_step {cfg.kb_expand_step}"\n          f"    {\'开\' if cfg.kb_enabled else \'关（KB_ENABLED=0）\'}")\n    print(f"  GRAPH_KB_ID        {cfg.graph_kb_id or \'(空)\'}    模式 {cfg.graph_mode}    "\n          f"{\'开\' if cfg.graph_enabled else \'关（GRAPH_ENABLED=0）\'}")\n    print(f"  超时               HTTP {cfg.http_timeout}s / 图谱 {cfg.graph_timeout}s")\n    print(f"  井控红线关键词     {\'、\'.join(cfg.guard_keywords) or \'(无)\'}")\n    print()\n\n\ndef check_reachable(cfg):\n    if not cfg.base_url:\n        print("[FAIL] 连通性：PLATFORM_BASE_URL 为空")\n        return False\n    try:\n        with opener(cfg).open(cfg.base_url, timeout=min(cfg.http_timeout, 10)) as resp:\n            print(f"[PASS] 连通性：{cfg.base_url} 返回 HTTP {resp.status}")\n        return True\n    except urllib.error.HTTPError as exc:\n        if exc.code in (502, 503, 504) and cfg.use_proxy:\n            print(f"[FAIL] 连通性：{cfg.base_url} 返回 HTTP {exc.code}；"\n                  "当前 PLATFORM_USE_PROXY=1，多半是本机代理把内网地址劫走了，设回 0 直连")\n            return False\n        print(f"[PASS] 连通性：{cfg.base_url} 返回 HTTP {exc.code}（能连上就算通）")\n        return True\n    except urllib.error.URLError as exc:\n        print(f"[FAIL] 连通性：连不上 {cfg.base_url} —— {exc.reason}（确认在内网/VPN 内）")\n        return False\n\n\ndef check_qa(cfg, question):\n    try:\n        result = qa_search(cfg, question, threshold=cfg.qa_threshold)\n    except (ConfigError, PlatformError) as exc:\n        print(f"[FAIL] 问答对检索：{exc}")\n        return False\n    state, top = result["state"], fmt_sim(result["top_similarity"])\n    if state == "hit":\n        print(f"[PASS] 问答对检索：命中 {len(result[\'hits\'])} 条，最高 {top}，类别 {result[\'hits\'][0][\'label\'] or \'未标注\'}")\n        return True\n    if state == "answer_null":\n        print(f"[FAIL] 问答对检索：召回到了（最高 {top}）但 answer 为 null —— 该类别没点「发布」")\n        return False\n    if state == "below_threshold":\n        print(f"[WARN] 问答对检索：接口通，但最高相似度 {top} < 阈值 {cfg.qa_threshold}（换个探针问题，或调阈值）")\n        return True\n    print(f"[WARN] 问答对检索：接口通，但零召回。确认 QA_ID_LIST 的 ID 正确且问答对已发布")\n    return True\n\n\ndef check_kb(cfg, question):\n    try:\n        result = kb_search(cfg, question)\n    except (ConfigError, PlatformError) as exc:\n        print(f"[FAIL] 知识库检索：{exc}")\n        return False\n    if result["state"] == "hit":\n        print(\n            f"[PASS] 知识库检索：命中 {len(result[\'chunks\'])} 段，最高 {result[\'top_score\']:.4f}，"\n            f"文档 {\'、\'.join(result[\'sources\']) or \'未标来源\'}"\n        )\n        return True\n    print("[WARN] 知识库检索：接口通但零片段。确认 KB_ID_LIST 是知识库 ID、文档已「保存并处理」入库；"\n          f"或探针问题本就离题（当前阈值 {cfg.kb_threshold}）")\n    return True\n\n\ndef check_graph(cfg, question):\n    try:\n        result = graph_search(cfg, question)\n    except (ConfigError, PlatformError) as exc:\n        print(f"[FAIL] 图谱检索：{exc}")\n        return False\n    if result["state"] == "hit":\n        print(\n            f"[PASS] 图谱检索：有答案（{len(result[\'answer\'])} 字），"\n            f"实体 {len(result[\'entities\'])} 关系 {len(result[\'relationships\'])}"\n            + ("" if result["done"] else "，但没收到 [DONE]")\n        )\n        return True\n    print("[WARN] 图谱检索：接口通但没给出答案。确认 GRAPH_KB_ID 是图谱库 ID 且已入库")\n    return True\n\n\ndef main():\n    use_utf8()\n    parser = argparse.ArgumentParser(description="配置与连通性自检")\n    parser.add_argument("-q", "--query", default="卡钻怎么处理？", help="探针问题")\n    parser.add_argument("--skip-fallback", "--skip-graph", dest="skip_fallback", action="store_true",\n                        help="只查连通性和问答对，不碰兜底级")\n    parser.add_argument("--env-file", default=None)\n    args = parser.parse_args()\n\n    try:\n        cfg = load_config(args.env_file)\n    except ConfigError as exc:\n        print(f"[FAIL] 读配置：{exc}")\n        print("[自检结果] FAIL —— 配置读不出来")\n        return 2\n\n    show_config(cfg)\n    results = [("连通性", check_reachable(cfg)), ("问答对", check_qa(cfg, args.query))]\n    if args.skip_fallback:\n        print("[SKIP] 兜底级（--skip-fallback）")\n    elif cfg.fallback == "kb":\n        print("[SKIP] 图谱检索（env.json 里 GRAPH_ENABLED=0）")\n        results.append(("知识库", check_kb(cfg, args.query)))\n    elif cfg.fallback == "graph":\n        print("[SKIP] 知识库检索（env.json 里 KB_ENABLED=0）")\n        results.append(("图谱", check_graph(cfg, args.query)))\n    else:\n        print("[SKIP] 兜底级：知识库和图谱都关着（问答对未命中会直接 no_answer）")\n\n    failed = [name for name, ok in results if not ok]\n    print()\n    if failed:\n        print(f"[自检结果] FAIL —— 未通过：{(\'、\'.join(failed))}（按上面每条的提示改 {cfg.env_file}）")\n        return 1\n    print(f"[自检结果] PASS —— {len(results)}/{len(results)} 项通过，可以跑 scripts/recommend.py")\n    return 0\n\n\nif __name__ == "__main__":\n    sys.exit(main())\n',
      },
      {
        path: "scripts/graph_search.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""只跑第二级检索：LightRAG 图谱检索（SSE）。\n\n一般流程请用 recommend.py。本脚本用于单独验证图谱库是否通、看实体与关系。\n\n  python3 scripts/graph_search.py -q "键槽卡钻怎么判断"\n  python3 scripts/graph_search.py -q "键槽卡钻怎么判断" --graph --mode hybrid\n"""\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parent))\n\nfrom platform_client import graph_search, load_config, run_cli\n\n\ndef render(result, with_graph=False):\n    state = "有答案" if result["state"] == "hit" else "图谱没给出答案"\n    print(f"[图谱] {state}｜kb_id={result[\'kb_id\']}｜模式 {result[\'query_mode\']}｜实体 {len(result[\'entities\'])} 关系 {len(result[\'relationships\'])}")\n    print(f"[检索问题] {result[\'question\']}")\n    if not result["done"]:\n        print("[⚠️] 没有收到 [DONE]，答案可能不完整（连接中断或超时，必要时调大 GRAPH_TIMEOUT）")\n    print("\\n────── 图谱答案 ──────")\n    print(result["answer"] or "(空)")\n\n    if with_graph and result["entities"]:\n        print("\\n[实体]")\n        for ent in result["entities"][:40]:\n            print(f"  {ent.get(\'id\')} {ent.get(\'entity\')}（{ent.get(\'type\')}）{(ent.get(\'description\') or \'\')[:60]}")\n        if result["relationships"]:\n            names = {str(e.get("id")): e.get("entity") for e in result["entities"]}\n            print("[关系]")\n            for rel in result["relationships"][:40]:\n                src = names.get(str(rel.get("source")), rel.get("source"))\n                dst = names.get(str(rel.get("target")), rel.get("target"))\n                print(f"  {src} → {dst}：{(rel.get(\'description\') or \'\')[:60]}")\n    for note in result["notes"]:\n        print(f"[备注] {note}")\n\n    print(\n        f"\\n[RESULT] stage=graph state={result[\'state\']} done={result[\'done\']} "\n        f"entities={len(result[\'entities\'])} relationships={len(result[\'relationships\'])}"\n    )\n\n\ndef main():\n    parser = argparse.ArgumentParser(description="图谱检索（第二级）")\n    parser.add_argument("-q", "--query", required=True, help="改写后的自洽问题")\n    parser.add_argument("--mode", default=None, help="query_mode，默认 env.json 的 GRAPH_QUERY_MODE（hybrid）")\n    parser.add_argument("--graph", action="store_true", help="打印实体与关系明细")\n    parser.add_argument("--timeout", type=float, default=None, help="秒，默认 env.json 的 GRAPH_TIMEOUT")\n    parser.add_argument("--json", action="store_true", help="输出 JSON")\n    parser.add_argument("--env-file", default=None)\n    args = parser.parse_args()\n\n    cfg = load_config(args.env_file)\n    result = graph_search(cfg, args.query, mode=args.mode, timeout=args.timeout)\n    if args.json:\n        print(json.dumps(result, ensure_ascii=False, indent=2))\n    else:\n        render(result, with_graph=args.graph)\n    return 0\n\n\nif __name__ == "__main__":\n    run_cli(main)\n',
      },
      {
        path: "scripts/kb_search.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""只跑兜底级之一：知识库向量检索（《钻井事故与复杂问题》的原文片段）。\n\n一般流程请用 recommend.py。本脚本用于单独验证知识库是否通、看片段与分值。\n\n  python3 scripts/kb_search.py -q "键槽卡钻怎么判断"\n  python3 scripts/kb_search.py -q "井漏了怎么处理" --k 5 --full\n\n**返回的是原文片段，不是成稿答案**：拿去回答用户前必须自己归纳，\n并写明「依据来自《钻井事故与复杂问题》」。\n"""\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parent))\n\nfrom platform_client import kb_search, load_config, run_cli\n\nPREVIEW_CHARS = 600\n\n\ndef render(result, full=False):\n    state = f"命中 {len(result[\'chunks\'])} 段" if result["state"] == "hit" else "零片段"\n    top = f"{result[\'top_score\']:.4f}" if result["top_score"] is not None else "—"\n    print(\n        f"[知识库] {state}｜最高 {top}｜阈值 {result[\'threshold\']}（服务端过滤）｜"\n        f"top_k {result[\'k\']}｜expand_step {result[\'expand_step\']}"\n    )\n    print(f"[检索问题] {result[\'question\']}")\n    if result["sources"]:\n        print(f"[命中文档] {\'、\'.join(result[\'sources\'])}")\n    if result["state"] != "hit":\n        print(\n            "\\n零片段 = 库里没有相关内容，或阈值偏高。"\n            "确认 KB_ID_LIST 是知识库 ID、文档已「保存并处理」入库；"\n            "调低 KB_THRESHOLD 前先想清楚：离题片段的分值也就 0.12–0.25。"\n        )\n    for i, chunk in enumerate(result["chunks"], 1):\n        head = f"\\n[片段 {i}] {chunk[\'score\']:.4f}｜{chunk[\'source\'] or \'未标来源\'}"\n        if chunk["page_no"]:\n            head += f"｜p.{chunk[\'page_no\']}"\n        print(head)\n        body = chunk["content"]\n        if not full and len(body) > PREVIEW_CHARS:\n            body = body[:PREVIEW_CHARS] + f"…（还有 {len(chunk[\'content\']) - PREVIEW_CHARS} 字，加 --full 看全文）"\n        print(body)\n\n    print(\n        f"\\n[RESULT] stage=kb state={result[\'state\']} top={top} "\n        f"chunks={len(result[\'chunks\'])} threshold={result[\'threshold\']}"\n    )\n\n\ndef main():\n    parser = argparse.ArgumentParser(description="知识库向量检索（兜底级之一）")\n    parser.add_argument("-q", "--query", required=True, help="改写后的自洽问题")\n    parser.add_argument("--threshold", type=float, default=None, help="默认读 env.json 的 KB_THRESHOLD（0.6）")\n    parser.add_argument("--k", type=int, default=None, help="片段数，默认 env.json 的 KB_TOP_K")\n    parser.add_argument("--expand-step", type=int, default=None, help="片段前后扩几块，默认 env.json 的 KB_EXPAND_STEP")\n    parser.add_argument("--full", action="store_true", help="打印片段全文（默认每段截到 600 字）")\n    parser.add_argument("--json", action="store_true", help="输出 JSON")\n    parser.add_argument("--env-file", default=None)\n    args = parser.parse_args()\n\n    cfg = load_config(args.env_file)\n    result = kb_search(cfg, args.query, threshold=args.threshold, k=args.k, expand_step=args.expand_step)\n    if args.json:\n        print(json.dumps(result, ensure_ascii=False, indent=2))\n    else:\n        render(result, full=args.full)\n    return 0\n\n\nif __name__ == "__main__":\n    run_cli(main)\n',
      },
      {
        path: "scripts/platform_client.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""井下作业措施推荐 —— 识油平台检索的共享客户端。\n\n只用标准库（沙箱 Python 3.11 与本机 3.14 都能跑），负责四件事：\n\n1. 读 `env.json`（skill 根目录；平台注册 skill 时会自动解析它并写进环境变量），\n   进程环境变量优先 —— 所以平台上跑和本地跑读到的是同一份配置；\n2. 拼装鉴权头 —— 2026-09-09 平台改版后**问答对与知识库不需要任何鉴权头**，\n   只有图谱（LightRAG）那一级还要 V-* + token；头名/前缀仍然做成可配，凭证空着就不发；\n3. 调 JSON 接口：问答对相似检索 `/backend/qa-algo-py/qa/robot_search`\n   与知识库向量检索 `/backend/chatpdf-algo4-py/vector/query_vector`；\n4. 调 SSE 接口：图谱检索 `/backend/lightrag/search`，把事件流解析成答案 + 图谱。\n\n问答对未命中后的**兜底级只能有一个**：知识库或图谱，或都不开（见 `Config.fallback`）。\n\n其余脚本只 import 本模块，不要各自写 HTTP。\n"""\nfrom __future__ import annotations\n\nimport json\nimport os\nimport re\nimport sys\nimport urllib.error\nimport urllib.request\nfrom pathlib import Path\n\nSKILL_ROOT = Path(__file__).resolve().parent.parent\n\nQA_PATH = "/backend/qa-algo-py/qa/robot_search"\n# 路由是 lightrag，**不是 lightrag-py**（QA/KB 那两个才带 -py）。\n# 写错路由时网关一律回 {"status":610,"message":"No authentication information ."}，\n# 看着像鉴权失败 —— 2026-09-10 之前就是这么被误判成「图谱要凭证」的。收到 610 先查这一行。\nGRAPH_PATH = "/backend/lightrag/search"\n# 这个接口不在官方接口文档里，是从平台前端抓的（契约见仓库 docs/检索接口.md ③）\nKB_PATH = "/backend/chatpdf-algo4-py/vector/query_vector"\n\nKNOWN_KEYS = (\n    "PLATFORM_BASE_URL",\n    "PLATFORM_TOKEN",\n    "PLATFORM_AUTH_HEADER",\n    "PLATFORM_AUTH_PREFIX",\n    "PLATFORM_COOKIE",\n    "PLATFORM_EXTRA_HEADERS",\n    "PLATFORM_USE_PROXY",\n    "QA_ID_LIST",\n    "QA_TYPE_ID",\n    "QA_THRESHOLD",\n    "QA_TOP_K",\n    "QA_PREFER_AGGREGATE",\n    "QA_AGGREGATE_MARGIN",\n    "GRAPH_KB_ID",\n    "GRAPH_QUERY_MODE",\n    "GRAPH_ENABLED",\n    "KB_ID_LIST",\n    "KB_ENABLED",\n    "KB_THRESHOLD",\n    "KB_TOP_K",\n    "KB_EXPAND_STEP",\n    "HTTP_TIMEOUT",\n    "GRAPH_TIMEOUT",\n    "GUARD_KEYWORDS",\n    "QA_DEFINITION_THRESHOLD",\n)\n\n\nclass PlatformError(RuntimeError):\n    """平台侧错误（鉴权、网络、响应格式）。退出码 3。"""\n\n\nclass ConfigError(RuntimeError):\n    """env.json 配置缺失或写错。退出码 2。"""\n\n\ndef use_utf8() -> None:\n    for stream in (sys.stdout, sys.stderr):\n        if hasattr(stream, "reconfigure"):\n            try:\n                stream.reconfigure(encoding="utf-8")\n            except Exception:\n                pass\n\n\n# --------------------------------------------------------------------------- env\n\n\ndef _parse_env_file(path: Path) -> dict:\n    values = {}\n    for raw in path.read_text(encoding="utf-8-sig").splitlines():\n        line = raw.strip()\n        if not line or line.startswith("#"):\n            continue\n        if line.startswith("export "):\n            line = line[len("export ") :].strip()\n        if "=" not in line:\n            continue\n        key, value = line.split("=", 1)\n        key, value = key.strip(), value.strip()\n        if len(value) >= 2 and value[0] == value[-1] and value[0] in ("\'", \'"\'):\n            value = value[1:-1]\n        values[key] = value\n    return values\n\n\ndef _parse_env_json(path: Path) -> dict:\n    """`env.json`：**所有环境变量的唯一存放处，里面只放环境变量**。\n\n    平台注册 skill 时会自动解析它、把每个键值写进进程环境变量；本地跑没人注入，脚本自己读同一份。\n    所以它必须是**扁平的 `{"变量名": "值"}`** —— 多写任何别的键（注释、分组、嵌套）都会被平台\n    当成环境变量写进去。变量的说明写在仓库的 `docs/环境变量.md`，不要写进这个文件。\n\n    值一律用字符串（环境变量本来就是字符串）；写成数字或布尔也认（布尔转 `"1"` / `"0"`）。\n    """\n    try:\n        data = json.loads(path.read_text(encoding="utf-8-sig"))\n    except json.JSONDecodeError as exc:\n        raise ConfigError(f"{path} 不是合法 JSON：{exc}")\n    if not isinstance(data, dict):\n        raise ConfigError(f"{path} 顶层必须是扁平 JSON 对象，如 {{\\"QA_THRESHOLD\\": \\"0.69\\"}}")\n\n    values = {}\n    for key, value in data.items():\n        if isinstance(value, bool):\n            values[key] = "1" if value else "0"\n        elif value is None:\n            values[key] = ""\n        elif isinstance(value, (int, float, str)):\n            values[key] = str(value)\n        else:\n            raise ConfigError(\n                f"{path} 里 {key} 的值是 {type(value).__name__} —— 环境变量只能是字符串"\n                "（值本身是 JSON 的，例如 PLATFORM_EXTRA_HEADERS，要写成转义后的字符串）"\n            )\n    return values\n\n\ndef _parse_config_file(path: Path) -> dict:\n    """按后缀分流：`.json` 走 env.json 解析，其余按 `KEY=VALUE`（旧的 env.json）解析。"""\n    return _parse_env_json(path) if path.suffix.lower() == ".json" else _parse_env_file(path)\n\n\ndef load_values(env_file: str | None = None) -> dict:\n    """合并 env.json 与环境变量；环境变量胜出（平台注入的就是环境变量，也便于临时覆盖）。"""\n    candidates = []\n    if env_file:\n        candidates.append(Path(env_file).expanduser())\n    elif os.environ.get("WIR_ENV_FILE"):\n        candidates.append(Path(os.environ["WIR_ENV_FILE"]).expanduser())\n    else:\n        # env.json 是正主；.env 只为旧部署留个后路\n        candidates.extend([\n            SKILL_ROOT / "env.json", Path.cwd() / "env.json",\n            SKILL_ROOT / ".env", Path.cwd() / ".env",\n        ])\n\n    values, used = {}, None\n    for path in candidates:\n        if path.is_file():\n            values.update(_parse_config_file(path))\n            used = str(path)\n            break\n    if env_file and used is None:\n        raise ConfigError(f"--env-file 指定的文件不存在：{env_file}")\n\n    for key in KNOWN_KEYS:\n        if os.environ.get(key):\n            values[key] = os.environ[key]\n    values["__env_file__"] = used or ""\n    return values\n\n\ndef _as_float(values, key, default):\n    raw = (values.get(key) or "").strip()\n    if not raw:\n        return default\n    try:\n        return float(raw)\n    except ValueError:\n        raise ConfigError(f"{key} 不是数字：{raw!r}")\n\n\ndef _as_int(values, key, default):\n    raw = (values.get(key) or "").strip()\n    if not raw:\n        return default\n    try:\n        return int(float(raw))\n    except ValueError:\n        raise ConfigError(f"{key} 不是整数：{raw!r}")\n\n\ndef _split_list(raw: str):\n    return [part.strip() for part in re.split(r"[,\\n;]+", raw or "") if part.strip()]\n\n\nclass Config:\n    """解析后的配置。每个检索前调用对应的 require_* 做前置校验。"""\n\n    def __init__(self, values: dict):\n        self.env_file = values.get("__env_file__") or "(未找到 env.json，全部取自环境变量)"\n        self.base_url = (values.get("PLATFORM_BASE_URL") or "").strip().rstrip("/")\n        self.token = (values.get("PLATFORM_TOKEN") or "").strip()\n        self.auth_header = (values.get("PLATFORM_AUTH_HEADER") or "Authorization").strip()\n        # 默认 Bearer：只有填了 token 才会用到（图谱那一级），而平台要的就是 Bearer。\n        # 网关哪天改成裸 token，在 env.json 里把它设成空串。\n        self.auth_prefix = values.get("PLATFORM_AUTH_PREFIX", "Bearer").strip()\n        self.cookie = (values.get("PLATFORM_COOKIE") or "").strip()\n\n        self.extra_headers = {}\n        raw_extra = (values.get("PLATFORM_EXTRA_HEADERS") or "").strip()\n        if raw_extra:\n            try:\n                parsed = json.loads(raw_extra)\n            except json.JSONDecodeError as exc:\n                raise ConfigError(f"PLATFORM_EXTRA_HEADERS 不是合法 JSON：{exc}")\n            if not isinstance(parsed, dict):\n                raise ConfigError("PLATFORM_EXTRA_HEADERS 必须是 JSON 对象，如 {\\"X-Tenant\\":\\"1\\"}")\n            self.extra_headers = {str(k): str(v) for k, v in parsed.items()}\n\n        # 平台是内网地址，本机开了代理（Clash 之类）会把请求劫走并返回 502 —— 默认绕开系统代理\n        self.use_proxy = (values.get("PLATFORM_USE_PROXY") or "0").strip() not in ("0", "false", "False", "no")\n        self._opener = None\n\n        self.qa_type_id = (values.get("QA_TYPE_ID") or "").strip()\n        self.qa_ids, self.qa_labels = [], {}\n        for item in _split_list(values.get("QA_ID_LIST", "")):\n            qa_id, _, label = item.partition("=")\n            qa_id, label = qa_id.strip(), label.strip()\n            if not qa_id:\n                continue\n            self.qa_ids.append(qa_id)\n            if label:\n                self.qa_labels[qa_id] = label\n\n        self.qa_threshold = _as_float(values, "QA_THRESHOLD", 0.7)\n        self.qa_top_k = _as_int(values, "QA_TOP_K", 5)\n        self.prefer_aggregate = (values.get("QA_PREFER_AGGREGATE") or "1").strip() not in ("0", "false", "False", "no")\n        self.aggregate_margin = _as_float(values, "QA_AGGREGATE_MARGIN", 0.05)\n\n        self.graph_kb_id = (values.get("GRAPH_KB_ID") or "").strip()\n        self.graph_mode = (values.get("GRAPH_QUERY_MODE") or "hybrid").strip()\n        # 两级兜底在代码里**都默认关**，漏写不会撞成「两个都开」的配置错误。\n        # 但 env.json 的约定是 KB_ENABLED / GRAPH_ENABLED **两个都写出来**，哪怕其中一个是 0 ——\n        # 兜底级用哪一个是整套部署最要紧的一个选择，写全了一眼可见、要换只是把两个值对调。\n        # 见 docs/环境变量.md。\n        self.graph_enabled = (values.get("GRAPH_ENABLED") or "0").strip() not in ("0", "false", "False", "no")\n\n        # 第二级兜底的另一个选项：知识库（《钻井事故与复杂问题》的原文片段，不是成稿答案）\n        self.kb_ids = _split_list(values.get("KB_ID_LIST", ""))\n        self.kb_enabled = (values.get("KB_ENABLED") or "0").strip() not in ("0", "false", "False", "no")\n        # 这个 score 不是问答对那套余弦：对题 0.93–0.996、离题 0.12–0.25，平台前端默认 0.6。\n        # 服务端就按它过滤，低于阈值的片段根本不返回。\n        self.kb_threshold = _as_float(values, "KB_THRESHOLD", 0.6)\n        self.kb_top_k = _as_int(values, "KB_TOP_K", 3)\n        # 只加长片段、不改分值（step 0/1/2 → 同一条 content 390/479/607 字）\n        self.kb_expand_step = _as_int(values, "KB_EXPAND_STEP", 1)\n\n        # 兜底级二选一：两个都开时不猜用户想要哪个，直接报错（比静默生效安全）\n        if self.kb_enabled and self.graph_enabled:\n            raise ConfigError(\n                "KB_ENABLED 和 GRAPH_ENABLED 只能开一个 —— 问答对未命中后的兜底级二选一，"\n                "现在两个都是 1。两边装的是同一本《钻井事故与复杂问题》，同时开会让答案来源说不清。"\n                "把其中一个设为 0（当前选型是图谱：GRAPH_ENABLED=1、KB_ENABLED=0，"\n                f"依据见仓库 docs/兜底级对比.md）。改这个文件：{self.env_file}"\n            )\n        self.fallback = "kb" if self.kb_enabled else ("graph" if self.graph_enabled else "none")\n\n        self.http_timeout = _as_float(values, "HTTP_TIMEOUT", 60)\n        self.graph_timeout = _as_float(values, "GRAPH_TIMEOUT", 180)\n        self.guard_keywords = _split_list(values.get("GUARD_KEYWORDS", "井喷,井口失控,井控失效"))\n        # 定义题（「什么是X」）单独抬高问答对阈值：台账记录的是事故处置、不是术语定义，\n        # 这类问题本该落到《钻井事故与复杂问题》那一级。实测「什么是键槽卡钻？」对卡钻聚合条目有 0.7822，\n        # 比好几条真正该命中的问法还高，靠通用阈值拦不掉。\n        self.definition_threshold = _as_float(values, "QA_DEFINITION_THRESHOLD", 0.90)\n\n    # -- 校验 -------------------------------------------------------------\n    def _require_base(self):\n        if not self.base_url:\n            raise ConfigError(f"PLATFORM_BASE_URL 没填。改这个文件：{self.env_file}")\n\n    def require_qa(self):\n        self._require_base()\n        if not self.qa_type_id and not self.qa_ids:\n            raise ConfigError(\n                "问答对检索要 QA_ID_LIST（多库，推荐）或 QA_TYPE_ID（单库），两者都空。"\n                f" 改这个文件：{self.env_file}"\n            )\n\n    def require_graph(self):\n        self._require_base()\n        if not self.graph_kb_id:\n            raise ConfigError(f"GRAPH_KB_ID 没填，图谱检索无法执行。改这个文件：{self.env_file}")\n\n    def require_kb(self):\n        self._require_base()\n        if not self.kb_ids:\n            raise ConfigError(f"KB_ID_LIST 没填，知识库检索无法执行。改这个文件：{self.env_file}")\n\n    # -- 展示 -------------------------------------------------------------\n    def masked_token(self):\n        if not self.token:\n            return "(空)"\n        if len(self.token) <= 8:\n            return "*" * len(self.token)\n        return f"{self.token[:4]}…{self.token[-4:]}（长度 {len(self.token)}）"\n\n    def label_for(self, qa_id):\n        if not qa_id:\n            return ""\n        qa_id = str(qa_id)\n        if qa_id in self.qa_labels:\n            return self.qa_labels[qa_id]\n        for key, label in self.qa_labels.items():\n            if key in qa_id or qa_id in key:\n                return label\n        return ""\n\n\ndef load_config(env_file: str | None = None) -> Config:\n    return Config(load_values(env_file))\n\n\n# -------------------------------------------------------------------------- HTTP\n\n\ndef opener(cfg: Config):\n    """默认绕开系统/环境变量里的代理；确实要走代理时在 env.json 设 PLATFORM_USE_PROXY=1。"""\n    if cfg._opener is None:\n        handlers = [] if cfg.use_proxy else [urllib.request.ProxyHandler({})]\n        cfg._opener = urllib.request.build_opener(*handlers)\n    return cfg._opener\n\n\ndef _headers(cfg: Config, accept: str) -> dict:\n    # 凭证留空就一个鉴权头都不发（问答对/知识库现在就是这样）；填了就带上（图谱那一级需要）\n    headers = {"Content-Type": "application/json", "Accept": accept}\n    if cfg.token:\n        headers[cfg.auth_header] = f"{cfg.auth_prefix} {cfg.token}" if cfg.auth_prefix else cfg.token\n    if cfg.cookie:\n        headers["Cookie"] = cfg.cookie\n    headers.update(cfg.extra_headers)\n    return headers\n\n\ndef _proxy_hint(cfg: Config) -> str:\n    if not cfg.use_proxy:\n        return ""\n    return (\n        "\\n当前 PLATFORM_USE_PROXY=1，请求走了系统代理。平台是内网地址，"\n        "本机的代理（Clash 之类）会把它劫走并返回 502 —— 把这项设回 0 直连。"\n    )\n\n\ndef _auth_hint(cfg: Config) -> str:\n    return (\n        "网关回了 610/611。**先别急着找 token —— 三级接口当前都不需要任何鉴权头**"\n        "（问答对、知识库、图谱都是不带 Authorization、不带 V-* 直接通）。\\n"\n        "  1. **首先查路由拼写。** 写错路由时网关同样回 610「No authentication information .」，"\n        "看着像鉴权失败其实不是 —— 图谱的路由是 /backend/lightrag/search，"\n        "曾经误写成 /backend/lightrag-py/search，白查了很久凭证（2026-09-10 定位）。\\n"\n        "     当前三条路由：\\n"\n        f"       问答对 {QA_PATH}\\n"\n        f"       图谱   {GRAPH_PATH}\\n"\n        f"       知识库 {KB_PATH}\\n"\n        "  2. 路由没错才考虑平台是否改回要鉴权：往 env.json 加 PLATFORM_TOKEN，"\n        "需要时再加 PLATFORM_EXTRA_HEADERS（形如 {\\"V-Access-Ticket\\":\\"…\\",\\"V-Client-Id\\":\\"…\\"}）；"\n        f"当前 token {cfg.masked_token()}，"\n        f"头名/前缀是 PLATFORM_AUTH_HEADER / PLATFORM_AUTH_PREFIX（{cfg.auth_header!r} + {cfg.auth_prefix!r}）。\\n"\n        "     token 来源：平台「知识管理 → 知识令牌」新建，或从浏览器抓一个 AT- 开头的会话 token（会过期）。\\n"\n        f"  配置文件：{cfg.env_file}"\n    )\n\n\ndef _check_envelope(cfg: Config, data):\n    """平台网关失败时返回 {\\"meta\\":{\\"success\\":false,...},\\"data\\":null}。"""\n    if not isinstance(data, dict):\n        return\n    meta = data.get("meta")\n    if not isinstance(meta, dict) or meta.get("success") is not False:\n        return\n    status = meta.get("status")\n    message = str(meta.get("message") or "").strip()\n    lines = [f"平台返回失败：status={status} message={message or \'(空)\'}"]\n    if status in (610, 611) or "authentication" in message.lower() or "鉴权" in message or "登录" in message:\n        lines.append(_auth_hint(cfg))\n    elif "问答对未找到" in message:\n        lines.append(\n            "向量库命中了但按 ID 回表取不到记录 —— 典型原因是问答对导入后没点「发布」，"\n            "或换文件时旧条目删了没发布、两套向量并存。五个类别都要发布，「未发布: N」必须归零。"\n        )\n    raise PlatformError("\\n".join(lines))\n\n\ndef post_json(cfg: Config, path: str, payload: dict, timeout: float | None = None):\n    url = f"{cfg.base_url}{path}"\n    body = json.dumps(payload, ensure_ascii=False).encode("utf-8")\n    request = urllib.request.Request(url, data=body, headers=_headers(cfg, "application/json"), method="POST")\n    try:\n        with opener(cfg).open(request, timeout=timeout or cfg.http_timeout) as resp:\n            raw = resp.read().decode("utf-8", "replace")\n    except urllib.error.HTTPError as exc:\n        detail = exc.read().decode("utf-8", "replace")[:800] if exc.fp else ""\n        extra = "\\n" + _auth_hint(cfg) if exc.code in (401, 403) else ""\n        hint = _proxy_hint(cfg) if exc.code in (502, 503, 504) else ""\n        raise PlatformError(f"HTTP {exc.code} {exc.reason} —— {url}\\n{detail}{extra}{hint}")\n    except urllib.error.URLError as exc:\n        raise PlatformError(\n            f"连不上平台 {url}：{exc.reason}。确认在内网/VPN 内，且 PLATFORM_BASE_URL 正确。{_proxy_hint(cfg)}"\n        )\n    try:\n        data = json.loads(raw)\n    except json.JSONDecodeError:\n        raise PlatformError(f"响应不是 JSON（前 400 字）：{raw[:400]}")\n    _check_envelope(cfg, data)\n    return data\n\n\ndef iter_sse(cfg: Config, path: str, payload: dict, timeout: float | None = None):\n    """逐条 yield SSE 的 data 负载（含 [START-SSE] 这类标记）。"""\n    url = f"{cfg.base_url}{path}"\n    body = json.dumps(payload, ensure_ascii=False).encode("utf-8")\n    request = urllib.request.Request(url, data=body, headers=_headers(cfg, "text/event-stream"), method="POST")\n    try:\n        with opener(cfg).open(request, timeout=timeout or cfg.graph_timeout) as resp:\n            for raw_line in resp:\n                line = raw_line.decode("utf-8", "replace").rstrip("\\r\\n")\n                if not line or line.startswith(":"):\n                    continue\n                if line.startswith("data:"):\n                    yield line[5:].lstrip(" ")\n                elif line.startswith(("event:", "id:", "retry:")):\n                    continue\n                else:\n                    # 非 SSE 正文（多半是网关的 JSON 错误体）\n                    yield line\n    except urllib.error.HTTPError as exc:\n        detail = exc.read().decode("utf-8", "replace")[:800] if exc.fp else ""\n        extra = "\\n" + _auth_hint(cfg) if exc.code in (401, 403) else ""\n        hint = _proxy_hint(cfg) if exc.code in (502, 503, 504) else ""\n        raise PlatformError(f"HTTP {exc.code} {exc.reason} —— {url}\\n{detail}{extra}{hint}")\n    except urllib.error.URLError as exc:\n        raise PlatformError(\n            f"连不上平台 {url}：{exc.reason}。确认在内网/VPN 内，且 PLATFORM_BASE_URL 正确。{_proxy_hint(cfg)}"\n        )\n\n\n# ---------------------------------------------------------------- 问答对相似检索\n\n\ndef _norm_answer(text):\n    return re.sub(r"\\s+", "", text or "")\n\n\n# 五个类别导进了同一个问答库，`qa_id` 分不出类别 —— 只能按答案正文判定。\n# 聚合条目以【结论】开头，单案例以【井号】(钻井/试修完井) 或【队号】(HSE/井场建设) 开头。\nCASE_MARKERS = ("【井号】", "【队号】")\nAGGREGATE_MARKERS = ("【结论】",)\nKIND_CN = {"aggregate": "聚合结论", "case": "单案例", "unknown": "未判定类型"}\n\n\ndef classify_answer(answer):\n    head = (answer or "").lstrip()[:40]\n    if any(m in head for m in CASE_MARKERS):\n        return "case"\n    if any(m in head for m in AGGREGATE_MARKERS):\n        return "aggregate"\n    return "unknown"\n\n\ndef answer_subject(answer):\n    """单案例答案里的井号或队号，用于在多条命中里区分是哪一口井。"""\n    # 井号里有空格（Koulele CE-19），分隔符是 U+3000 全角空格，不能按普通空格截断\n    m = re.search(r"【(?:井号|队号)】\\s*([^【\\u3000\\r\\n]+)", answer or "")\n    return m.group(1).strip() if m else ""\n\n\ndef is_aggregate(item):\n    return item.get("kind") == "aggregate" or "综合结论" in (item.get("label") or "")\n\n\ndef describe(item):\n    """一行来源标识：聚合结论 / 单案例 · Koulele CE-19（有类别标注时附在后面）。"""\n    parts = [KIND_CN.get(item.get("kind"), "未判定类型")]\n    if item.get("subject"):\n        parts.append(item["subject"])\n    text = " · ".join(parts)\n    label = item.get("label")\n    if label and label not in text:\n        text += f"（{label}）"\n    return text\n\n\ndef qa_search(cfg: Config, question: str, threshold: float | None = None, k: int | None = None) -> dict:\n    """问答对相似检索。返回统一结构，state ∈ hit/answer_null/below_threshold/empty。"""\n    cfg.require_qa()\n    threshold = cfg.qa_threshold if threshold is None else float(threshold)\n    k = cfg.qa_top_k if k is None else int(k)\n\n    payload = {"input_question": question, "k": k}\n    if cfg.qa_type_id:\n        payload["type_id"] = cfg.qa_type_id\n        scope = f"单库 type_id={cfg.qa_type_id}"\n    else:\n        payload["qa_id_list"] = list(cfg.qa_ids)\n        scope = f"多库 {len(cfg.qa_ids)} 个问答库"\n\n    data = post_json(cfg, QA_PATH, payload)\n    status = str(data.get("status", ""))\n    raw_items = data.get("answer_id") or []\n\n    # 同一主问答会通过多个扩展问被召回多次 —— 按答案正文去重，保留最高相似度。\n    merged = {}\n    for item in raw_items:\n        if not isinstance(item, dict):\n            continue\n        answer = item.get("answer")\n        try:\n            similarity = float(item.get("similarity") or 0.0)\n        except (TypeError, ValueError):\n            similarity = 0.0\n        qa_id = item.get("qa_id") or cfg.qa_type_id or ""\n        key = _norm_answer(answer) or f"__null__{item.get(\'id\')}"\n        hit = merged.get(key)\n        if hit is None:\n            merged[key] = {\n                "id": item.get("id"),\n                "similarity": similarity,\n                "answer": answer,\n                "qa_id": qa_id,\n                "label": cfg.label_for(qa_id),\n                "kind": classify_answer(answer),\n                "subject": answer_subject(answer),\n                "recall_count": 1,\n            }\n        else:\n            hit["recall_count"] += 1\n            if similarity > hit["similarity"]:\n                hit.update({"id": item.get("id"), "similarity": similarity, "qa_id": qa_id, "label": cfg.label_for(qa_id)})\n\n    items = sorted(merged.values(), key=lambda x: x["similarity"], reverse=True)\n    above = [it for it in items if it["similarity"] >= threshold]\n    hits = [it for it in above if (it["answer"] or "").strip()]\n    nulls = [it for it in above if not (it["answer"] or "").strip()]\n\n    if hits:\n        state = "hit"\n    elif nulls:\n        state = "answer_null"\n    elif items:\n        state = "below_threshold"\n    else:\n        state = "empty"\n\n    promoted = False\n    if state == "hit" and cfg.prefer_aggregate and len(hits) > 1 and not _mentions_well(question):\n        primary = hits[0]\n        if not is_aggregate(primary):\n            for candidate in hits[1:]:\n                if is_aggregate(candidate) and candidate["similarity"] >= primary["similarity"] - cfg.aggregate_margin:\n                    hits.remove(candidate)\n                    hits.insert(0, candidate)\n                    promoted = True\n                    break\n\n    return {\n        "stage": "qa",\n        "state": state,\n        "question": question,\n        "scope": scope,\n        "threshold": threshold,\n        "k": k,\n        "platform_status": status,\n        "top_similarity": items[0]["similarity"] if items else None,\n        "hits": hits,\n        "nulls": nulls,\n        "all": items,\n        "raw_count": len(raw_items),\n        "promoted_aggregate": promoted,\n    }\n\n\n_WELL_PATTERNS = (\n    re.compile(r"[A-Za-z][A-Za-z]*(?:\\s+[A-Za-z]+)*\\s*-\\s*\\d+"),  # Sokor-1 / Koulele CE-19\n    re.compile(r"GW\\s*\\d{2,3}", re.IGNORECASE),                    # 队号 GW216\n)\n\n\ndef _mentions_well(question: str) -> bool:\n    return any(p.search(question or "") for p in _WELL_PATTERNS)\n\n\n# ------------------------------------------------------------------- 图谱检索\n\n\ndef strip_br(text: str) -> str:\n    return re.sub(r"<br\\s*/?>", "\\n", text or "")\n\n\n# 图谱是生成式的：答不上来时它不会返回空，而是**用一段通顺的中文说自己答不了**\n# （实测「差旅报销流程是什么」→「根据提供的知识库内容，无法回答关于…的问题」，还附 29 个实体）。\n# 知识库那一级靠服务端阈值过滤，落空就是 chunks=0；图谱没有 score，只能认措辞。\n# 不识别的话 recommend.py 会判 use_graph_answer，把「我答不了」当答案讲给用户。\n# 这五条是拿 tools/fallback-cases.txt 的 20 条题实测标定的（2026-09-10）：\n# 7 条该判拒答的全中（含「描述较少」那种先示弱、后拿邻近话题硬凑的），\n# 13 条正常答案零误判。改这里要重跑 tools/compare_fallback.py 复核。\n_GRAPH_REFUSAL_PATTERNS = (\n    # 「无法回答」「无法直接提供」「无法准确给出」\n    re.compile(r"无法(直接|准确|明确)?(回答|提供|给出|得出)"),\n    re.compile(r"不足以回答"),\n    # 先说「描述较少」再长篇讲别的 —— 实测「项目部的组织架构」就是这样\n    # 拿《钻井事故与复杂问题》里的「井喷抢险指挥部」凑了一篇，是最危险的一种假命中\n    re.compile(r"(描述|记载|信息|内容)较少"),\n    # 中间要能夹副词：实测同一个问题 4 次里 3 次说的是「并未**直接**提及」\n    re.compile(r"(并未|未|没有)[^。，]{0,4}(包含|提及|涉及|收录|记录|说明|描述|给出)"),\n    re.compile(r"(知识库|文档|资料|提供的(上下文|内容|信息))[^。]{0,12}(并未|未|没有)"),\n)\n\n# 只看开头这么多字：拒答一定写在第一句，正文里偶尔出现「未提及」不该被误判成拒答\n_REFUSAL_SCAN_CHARS = 200\n\n\ndef is_graph_refusal(answer: str) -> bool:\n    """图谱那段话是不是在说「我答不了」。"""\n    head = (answer or "").strip()[:_REFUSAL_SCAN_CHARS]\n    return any(p.search(head) for p in _GRAPH_REFUSAL_PATTERNS)\n\n\ndef graph_search(cfg: Config, query: str, mode: str | None = None, timeout: float | None = None) -> dict:\n    """LightRAG 图谱检索，解析 SSE 事件流。"""\n    cfg.require_graph()\n    mode = mode or cfg.graph_mode\n    payload = {"query": query, "query_mode": mode, "kb_id": cfg.graph_kb_id}\n\n    section = None\n    answer_parts, json_parts, stray = [], [], []\n    done = False\n    for chunk in iter_sse(cfg, GRAPH_PATH, payload, timeout=timeout):\n        if chunk == "[DONE]":\n            done = True\n            break\n        if chunk == "[START-SSE]":\n            section = "sse"\n        elif chunk == "[END-SSE]":\n            section = None\n        elif chunk == "[START-JSON]":\n            section = "json"\n        elif chunk == "[END-JSON]":\n            section = None\n        elif section == "sse":\n            answer_parts.append(chunk)\n        elif section == "json":\n            json_parts.append(chunk)\n        elif chunk:\n            stray.append(chunk)\n\n    graph, entities, relationships, text_units = None, [], [], []\n    if json_parts:\n        try:\n            graph = json.loads("".join(json_parts))\n        except json.JSONDecodeError:\n            stray.append("图谱 JSON 解析失败（已忽略，答案仍可用）")\n        if isinstance(graph, dict):\n            payload_data = graph.get("data") if isinstance(graph.get("data"), dict) else graph\n            entities = payload_data.get("entities") or []\n            relationships = payload_data.get("relationships") or []\n            text_units = payload_data.get("text_units") or []\n\n    answer = strip_br("".join(answer_parts)).strip()\n    if not answer and stray:\n        joined = " ".join(stray)\n        # 网关的错误体（610/611 鉴权之类）是普通 JSON、不是 SSE —— 过一遍信封检查，\n        # 这样图谱这一级也能拿到和 JSON 接口一样的排查提示，而不是干巴巴一句「没返回答案」\n        try:\n            _check_envelope(cfg, json.loads(joined))\n        except json.JSONDecodeError:\n            pass\n        raise PlatformError(f"图谱接口没有返回答案，响应正文：{joined[:600]}")\n\n    refused = is_graph_refusal(answer)\n    if refused:\n        stray = stray + ["图谱明确表示答不了（按拒答措辞判为落空，不当作命中）"]\n\n    return {\n        "stage": "graph",\n        "state": "hit" if (answer and not refused) else "empty",\n        "refused": refused,\n        "raw_state": "hit" if answer else "empty",\n        "question": query,\n        "kb_id": cfg.graph_kb_id,\n        "query_mode": mode,\n        "answer": answer,\n        "entities": entities,\n        "relationships": relationships,\n        "text_units": text_units,\n        "done": done,\n        "notes": stray,\n    }\n\n\n# ------------------------------------------------------------------- 知识库检索\n\n\nFALLBACK_CN = {"kb": "知识库", "graph": "图谱库", "none": "关闭（问答对未命中直接 no_answer）"}\n\n# 说明：《钻井事故与复杂问题》里的「例X」现场实例章节留在知识库里，不做剔除、也不单独打标 ——\n# 风险由「来源标注」兜住：答案必须说清是尼日尔历史记录还是《钻井事故与复杂问题》（SKILL.md「统一回答结构」）。\n\n\ndef kb_search(cfg: Config, query: str, threshold: float | None = None,\n              k: int | None = None, expand_step: int | None = None) -> dict:\n    """知识库向量检索。返回的是**《钻井事故与复杂问题》的原文片段，不是成稿答案** —— 归纳交给 agent。\n\n    契约见仓库 `docs/检索接口.md` ③。两个要点：\n    `threshold` 是**服务端**过滤（低于它的片段根本不返回），`expand_step` 只加长片段不改分值。\n    """\n    cfg.require_kb()\n    threshold = cfg.kb_threshold if threshold is None else float(threshold)\n    k = cfg.kb_top_k if k is None else int(k)\n    expand_step = cfg.kb_expand_step if expand_step is None else int(expand_step)\n\n    payload = {\n        "query": query,\n        "kb_name": list(cfg.kb_ids),\n        "top_k": k,\n        "threshold": threshold,\n        "expand_step": expand_step,\n    }\n    data = post_json(cfg, KB_PATH, payload)\n    raw = data.get("query_result") if isinstance(data, dict) else None\n    if raw is None:\n        raise PlatformError(\n            f"知识库响应里没有 query_result 字段（前 300 字）：{str(data)[:300]}"\n        )\n\n    seen, chunks = set(), []\n    for item in raw:\n        if not isinstance(item, dict):\n            continue\n        content = (item.get("content") or "").strip()\n        if not content:\n            continue\n        key = _norm_answer(content)\n        if key in seen:          # 同一段可能被多次召回\n            continue\n        seen.add(key)\n        try:\n            score = float(item.get("score") or 0.0)\n        except (TypeError, ValueError):\n            score = 0.0\n        chunks.append({\n            "content": content,\n            "score": score,\n            "source": item.get("source") or "",\n            "page_no": item.get("page_no") or "",\n            "file_id": item.get("file_id") or "",\n        })\n    chunks.sort(key=lambda c: c["score"], reverse=True)\n\n    sources = []\n    for chunk in chunks:\n        if chunk["source"] and chunk["source"] not in sources:\n            sources.append(chunk["source"])\n\n    return {\n        "stage": "kb",\n        "state": "hit" if chunks else "empty",\n        "question": query,\n        "kb_ids": list(cfg.kb_ids),\n        "threshold": threshold,\n        "k": k,\n        "expand_step": expand_step,\n        "chunks": chunks,\n        "sources": sources,\n        "top_score": chunks[0]["score"] if chunks else None,\n        "raw_count": len(raw),\n    }\n\n\n# ----------------------------------------------------------------------- 其他\n\n\ndef guard_hits(cfg: Config, question: str):\n    return [kw for kw in cfg.guard_keywords if kw and kw in (question or "")]\n\n\n# 「是什么问题」这类不算——倒划眼憋停那条征兆描述就是以「是什么问题？」结尾的\n_DEFINITION_PATTERNS = (\n    re.compile(r"^\\s*(什么是|什么叫|何谓|何为|啥是|啥叫)"),\n    re.compile(r"(是什么意思|的定义是|的定义\\s*[?？]?\\s*$|怎么定义)"),\n)\n\n\ndef is_definition(question: str) -> bool:\n    """定义题 = 问术语含义，不是问怎么处置。"""\n    return any(p.search(question or "") for p in _DEFINITION_PATTERNS)\n\n\ndef fmt_sim(value):\n    return "—" if value is None else f"{value:.4f}"\n\n\ndef run_cli(main):\n    """统一错误出口：2=配置错，3=平台/网络错。结论行同时落在首行与末行。"""\n    use_utf8()\n    try:\n        sys.exit(main())\n    except ConfigError as exc:\n        print(f"[配置错误] {exc}", file=sys.stderr)\n        print("[RESULT] error=config", file=sys.stderr)\n        sys.exit(2)\n    except PlatformError as exc:\n        print(f"[平台错误] {exc}", file=sys.stderr)\n        print("[RESULT] error=platform", file=sys.stderr)\n        sys.exit(3)\n    except KeyboardInterrupt:\n        sys.exit(130)\n',
      },
      {
        path: "scripts/qa_search.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""只跑第一级检索：问答对相似检索。\n\n一般流程请直接用 recommend.py（它会在未命中时自动接兜底级：知识库或图谱）。\n本脚本用于单独看问答对的召回明细、调阈值、排查「命中但 answer 为 null」。\n\n  python3 scripts/qa_search.py -q "卡钻怎么处理？"\n  python3 scripts/qa_search.py -q "卡钻怎么处理？" --threshold 0.6 --k 10 --show-all\n"""\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parent))\n\nfrom platform_client import describe, fmt_sim, load_config, qa_search, run_cli\n\nSTATE_TEXT = {\n    "hit": "命中",\n    "answer_null": "召回到了但答案为空（问答对大概率没发布）",\n    "below_threshold": "未命中（全部低于阈值）",\n    "empty": "未命中（向量库零召回）",\n}\n\n\ndef preview(text, width=70):\n    flat = " ".join((text or "").split())\n    return flat[:width] + ("…" if len(flat) > width else "")\n\n\ndef render(result, show_all=False, full=True):\n    state = result["state"]\n    head = f"[QA] {STATE_TEXT[state]}｜阈值 {result[\'threshold\']:.2f}｜最高相似度 {fmt_sim(result[\'top_similarity\'])}"\n    if result["hits"]:\n        head += f"｜{describe(result[\'hits\'][0])}"\n    print(head)\n    print(f"[检索问题] {result[\'question\']}")\n    print(f"[检索范围] {result[\'scope\']}，k={result[\'k\']}，原始召回 {result[\'raw_count\']} 条 → 去重 {len(result[\'all\'])} 条")\n    if result["promoted_aggregate"]:\n        print("[排序调整] 问题没带井名，已把「综合结论」聚合条目提到第一位（单案例条目相似度相近时会抢命中）")\n\n    for idx, hit in enumerate(result["hits"], 1):\n        print(\n            f"\\n────── #{idx} 相似度 {hit[\'similarity\']:.4f} │ {describe(hit)} │ QA-ID {hit[\'id\']} │ 召回 {hit[\'recall_count\']} 次 ──────"\n        )\n        print((hit["answer"] or "").strip() if full else preview(hit["answer"], 200))\n\n    if result["nulls"]:\n        print("\\n[⚠️ 答案为空的召回]")\n        for hit in result["nulls"]:\n            print(f"  {hit[\'similarity\']:.4f} QA-ID {hit[\'id\']}（答案为空，判不出类型）")\n        print("  → 向量命中但回表取不到主问答：检查该类别是否点了「发布」（平台部署说明 §8.4）")\n\n    rest = [it for it in result["all"] if it not in result["hits"] and it not in result["nulls"]]\n    if rest and (show_all or state != "hit"):\n        print(f"\\n[阈值以下的召回]（共 {len(rest)} 条，调阈值时看这里）")\n        for hit in rest[: (len(rest) if show_all else 5)]:\n            print(f"  {hit[\'similarity\']:.4f} {describe(hit)} │ {preview(hit[\'answer\'])}")\n\n    print(\n        f"\\n[RESULT] stage=qa state={state} top={fmt_sim(result[\'top_similarity\'])} "\n        f"hits={len(result[\'hits\'])} nulls={len(result[\'nulls\'])} threshold={result[\'threshold\']:.2f} "\n        f"platform_status={result[\'platform_status\'] or \'?\'}"\n    )\n\n\ndef main():\n    parser = argparse.ArgumentParser(description="问答对相似检索（第一级）")\n    parser.add_argument("-q", "--query", required=True, help="改写后的自洽问题")\n    parser.add_argument("--threshold", type=float, default=None, help="相似度阈值，默认读 env.json 的 QA_THRESHOLD")\n    parser.add_argument("--k", type=int, default=None, help="召回条数，默认读 env.json 的 QA_TOP_K")\n    parser.add_argument("--show-all", action="store_true", help="把阈值以下的召回全部列出")\n    parser.add_argument("--preview-only", action="store_true", help="答案只打摘要，不打全文")\n    parser.add_argument("--json", action="store_true", help="输出 JSON")\n    parser.add_argument("--env-file", default=None)\n    args = parser.parse_args()\n\n    cfg = load_config(args.env_file)\n    result = qa_search(cfg, args.query, threshold=args.threshold, k=args.k)\n    if args.json:\n        print(json.dumps(result, ensure_ascii=False, indent=2))\n    else:\n        render(result, show_all=args.show_all, full=not args.preview_only)\n    return 0\n\n\nif __name__ == "__main__":\n    run_cli(main)\n',
      },
      {
        path: "scripts/recommend.py",
        encoding: "utf8",
        executable: true,
        data: '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""措施推荐的主入口：问答对 →（未命中才）兜底级，命中即退出。\n\n兜底级是**知识库或图谱，二选一**（`env.json` 的 KB_ENABLED / GRAPH_ENABLED，两个都开会报配置错误），\n也可以都关。当前部署：知识库开、图谱关（图谱库还在入库）。\n\n这是 SKILL.md 里唯一需要照抄的命令。传进来的 --query 必须是**已改写好的自洽问题**\n（多轮对话里补全主语/井号/事故类型），脚本不做改写。\n\n  python3 scripts/recommend.py -q "卡钻怎么处理？"\n  python3 scripts/recommend.py -q "Koulele CE-19 2021年10月卡钻是怎么处理的？" --threshold 0.72\n  python3 scripts/recommend.py -q "键槽卡钻怎么处理" --source kb    # 用户点名要查《钻井事故与复杂问题》，跳过问答对\n  python3 scripts/recommend.py -q "什么是键槽卡钻" --json\n\n结论行同时打在**第一行和最后一行**（平台截断输出时保首尾）。\n"""\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path\n\nsys.path.insert(0, str(Path(__file__).resolve().parent))\n\nfrom platform_client import (\n    FALLBACK_CN,\n    PlatformError,\n    describe,\n    fmt_sim,\n    graph_search,\n    guard_hits,\n    is_definition,\n    kb_search,\n    load_config,\n    qa_search,\n    run_cli,\n)\n\nRULE = "─" * 58\n# 知识库片段整体最多打这么多字，超了让人去跑 kb_search.py --full（片段是《钻井事故与复杂问题》原文，可能很长）\nKB_PRINT_BUDGET = 6000\n\n\ndef preview(text, width=64):\n    flat = " ".join((text or "").split())\n    return (flat[:width] + ("…" if len(flat) > width else "")) if flat else "(答案为空)"\n\n\ndef run_fallback(cfg, level, args):\n    """跑《钻井事故与复杂问题》那一级（知识库或图谱），返回 (kb, graph, kb_error, graph_error)。"""\n    kb = graph = kb_error = graph_error = None\n    if level == "kb":\n        try:\n            kb = kb_search(cfg, args.query, threshold=args.kb_threshold)\n        except PlatformError as exc:\n            kb_error = str(exc)\n    else:\n        try:\n            graph = graph_search(cfg, args.query, mode=args.mode)\n        except PlatformError as exc:\n            graph_error = str(exc)\n    return kb, graph, kb_error, graph_error\n\n\ndef print_kb(kb, query):\n    print(\n        f"\\n[来源] 知识库（《钻井事故与复杂问题》）│ 命中 {len(kb[\'chunks\'])} 段 │ "\n        f"最高 {kb[\'top_score\']:.4f} │ 文档 {\'、\'.join(kb[\'sources\']) or \'未标来源\'} │ "\n        f"阈值 {kb[\'threshold\']}（服务端过滤）"\n    )\n    print("[提醒] 以下是**《钻井事故与复杂问题》的原文片段，不是答案**：自己归纳成结论 + 步骤，"\n          "并在回答里写明依据来自《钻井事故与复杂问题》")\n    budget = KB_PRINT_BUDGET\n    for i, chunk in enumerate(kb["chunks"], 1):\n        if budget <= 0:\n            print(f"\\n[片段 {i}–{len(kb[\'chunks\'])}] 未打印（超出输出预算），需要就跑："\n                  f"python3 scripts/kb_search.py -q \\"{query}\\" --full")\n            break\n        body = chunk["content"]\n        if len(body) > budget:\n            body = body[:budget] + "…（截断，全文用 kb_search.py --full）"\n        budget -= len(body)\n        print(f"\\n{RULE}\\n[片段 {i}] {chunk[\'score\']:.4f} │ {chunk[\'source\'] or \'未标来源\'}\\n{body}\\n{RULE}")\n\n\ndef print_graph(graph):\n    print(\n        f"\\n[来源] 图谱库（《钻井事故与复杂问题》）│ kb_id={graph[\'kb_id\']} │ "\n        f"模式 {graph[\'query_mode\']} │ 实体 {len(graph[\'entities\'])} 关系 {len(graph[\'relationships\'])}"\n        + ("" if graph["done"] else " │ ⚠️ 未收到 [DONE]，答案可能被截断")\n    )\n    print(f"\\n{RULE}\\n{graph[\'answer\'] or \'(图谱没有给出答案)\'}\\n{RULE}")\n    names = [str(e.get("entity")) for e in graph["entities"][:12] if e.get("entity")]\n    if names:\n        print(f"[图谱实体] {\'、\'.join(names)}")\n\n\ndef main():\n    parser = argparse.ArgumentParser(description="井下作业措施推荐：问答对 → 兜底级，命中即退出")\n    parser.add_argument("-q", "--query", required=True, help="已改写为自洽的检索问题（不要直接塞用户原话的追问）")\n    parser.add_argument("--source", choices=("auto", "qa", "kb", "graph"), default="auto",\n                        help="auto=问答对→兜底级（默认）；qa=只查问答对；"\n                             "kb/graph=**跳过问答对**直接查该库（仅在用户点名要查《钻井事故与复杂问题》/图谱时用）")\n    parser.add_argument("--threshold", type=float, default=None, help="问答对相似度阈值，默认读 env.json 的 QA_THRESHOLD")\n    parser.add_argument("--k", type=int, default=None, help="问答对召回条数，默认读 env.json 的 QA_TOP_K")\n    parser.add_argument("--kb-threshold", type=float, default=None, help="知识库阈值，默认读 env.json 的 KB_THRESHOLD")\n    parser.add_argument("--no-fallback", "--no-graph", dest="no_fallback", action="store_true",\n                        help="等同 --source qa：问答对未命中就到此为止")\n    parser.add_argument("--no-guard", action="store_true", help="关掉井控红线拦截（默认开）")\n    parser.add_argument("--mode", default=None, help="图谱 query_mode，默认 hybrid")\n    parser.add_argument("--json", action="store_true", help="输出 JSON（给脚本用，人读用默认格式）")\n    parser.add_argument("--env-file", default=None)\n    args = parser.parse_args()\n\n    cfg = load_config(args.env_file)\n    guards = [] if args.no_guard else guard_hits(cfg, args.query)\n    source = "qa" if args.no_fallback else args.source\n\n    qa = kb = graph = kb_error = graph_error = None\n    definitional = False\n    threshold = None\n    forced_note = ""\n\n    # ---- 决策 --------------------------------------------------------------\n    if source in ("kb", "graph"):\n        # 用户点名要查《钻井事故与复杂问题》/图谱 → 跳过问答对。env.json 里关着也照查，但要说明。\n        level_cn = FALLBACK_CN[source]\n        enabled = cfg.kb_enabled if source == "kb" else cfg.graph_enabled\n        if not enabled:\n            key = "KB_ENABLED" if source == "kb" else "GRAPH_ENABLED"\n            forced_note = (f"[提示] env.json 里这一级是关着的（{key}=0），本次因为点名指定才查；"\n                           "结果可能不可靠，回答时要提醒用户")\n        kb, graph, kb_error, graph_error = run_fallback(cfg, source, args)\n        got = bool((kb and kb["state"] == "hit") or (graph and graph["state"] == "hit"))\n        failed = bool(kb_error or graph_error)\n\n        if got:\n            decision = "use_kb_answer" if source == "kb" else "use_graph_answer"\n            what = ("下面给的是**《钻井事故与复杂问题》的原文片段，不是写好的答案**：按 SKILL.md「统一回答结构」"\n                    "归纳成【结论】+【处置步骤】" if source == "kb" else "照图谱答案回答")\n            headline = (\n                f"[决策] 按要求**跳过问答对**，直接查{level_cn}（《钻井事故与复杂问题》）→ {what}。"\n                "回答里必须写明「依据来自《钻井事故与复杂问题》」，"\n                "并告诉用户默认流程会先查历史记录，需要的话可以再问一次"\n            )\n        else:\n            decision = "no_answer"\n            why = f"{level_cn}调用失败" if failed else f"{level_cn}里没有相关内容"\n            headline = (\n                f"[决策] 按要求跳过问答对直接查{level_cn}，但{why} → 不要编造措施。"\n                "告诉用户《钻井事故与复杂问题》这一级没有对应内容，并建议回到默认流程（先查尼日尔历史记录）"\n            )\n    else:\n        # 定义题（「什么是X」）单独抬高阈值：台账记录事故处置、不记术语定义，这类问题该落到《钻井事故与复杂问题》那一级\n        definitional = is_definition(args.query)\n        threshold_in = cfg.qa_threshold if args.threshold is None else args.threshold\n        if definitional:\n            threshold_in = max(threshold_in, cfg.definition_threshold)\n\n        qa = qa_search(cfg, args.query, threshold=threshold_in, k=args.k)\n        threshold = qa["threshold"]\n        top = fmt_sim(qa["top_similarity"])\n        fallback = "none" if source == "qa" else cfg.fallback\n\n        if qa["state"] == "hit":\n            primary = qa["hits"][0]\n            decision = "use_qa_answer"\n            headline = (\n                f"[决策] 问答对命中（相似度 {primary[\'similarity\']:.4f}，{describe(primary)}）"\n                "→ 把下面的「答案正文」原样输出给用户，不要改写、不要再补《钻井事故与复杂问题》的内容、不要追加追问示例"\n            )\n        else:\n            if qa["state"] == "answer_null":\n                reason = f"问答对召回到了（最高 {top} ≥ 阈值 {threshold:.2f}）但 answer 为空 —— 该类别没点「发布」"\n            elif qa["state"] == "below_threshold":\n                reason = f"问答对未命中（最高 {top} < 阈值 {threshold:.2f}）"\n            else:\n                reason = "问答对未命中（向量库零召回）"\n\n            if fallback == "none":\n                why = ("指定了 --source qa" if source == "qa"\n                       else "两级兜底都关着（env.json 里 KB_ENABLED=0、GRAPH_ENABLED=0）")\n                decision = "no_answer"\n                headline = f"[决策] {reason}，且{why} → 不要编造措施，如实说明台账里没有同类记录"\n            else:\n                level_cn = FALLBACK_CN[fallback]\n                kb, graph, kb_error, graph_error = run_fallback(cfg, fallback, args)\n                got = bool((kb and kb["state"] == "hit") or (graph and graph["state"] == "hit"))\n                failed = bool(kb_error or graph_error)\n\n                if got and fallback == "kb":\n                    decision = "use_kb_answer"\n                    headline = (\n                        f"[决策] {reason} → 已转知识库。下面给的是**《钻井事故与复杂问题》的原文片段，不是写好的答案**："\n                        "按 SKILL.md「统一回答结构」归纳成【结论】+【处置步骤】，"\n                        "必须写明「依据来自《钻井事故与复杂问题》」"\n                        "（【依据】就写书名，不写「图谱」「知识库」，也不补「台账里没有」）"\n                    )\n                elif got:\n                    decision = "use_graph_answer"\n                    headline = (\n                        f"[决策] {reason} → 已转知识图谱。图谱答案来自《钻井事故与复杂问题》构建的本体图谱："\n                        "回答时必须写明「依据来自《钻井事故与复杂问题》」"\n                        "（【依据】就写书名，不写「图谱」「知识库」，也不补「台账里没有」）"\n                    )\n                else:\n                    decision = "no_answer"\n                    tail_reason = f"{level_cn}调用失败" if failed else f"{level_cn}也没有给出内容"\n                    headline = (\n                        f"[决策] {reason}，{tail_reason} → 不要编造措施，"\n                        "向用户说明没有同类记录，请其补充井号/井深/工况，或转人工"\n                    )\n\n    detail_line = ""\n    if guards:\n        decision = "route_to_well_control"\n        detail_line = headline.replace("[决策]", "[检索结论]", 1)\n        headline = (\n            "[决策] 命中井控红线 → **不做措施推荐**：回答「立即执行井控程序（按设计关井、压井）、"\n            "上报值班、转人工」。下面的检索结果只作背景，不得当作本项目处置经验"\n        )\n\n    if args.json:\n        print(json.dumps(\n            {\n                "query": args.query,\n                "decision": decision,\n                "source": source,\n                "guards": guards,\n                "definitional": definitional,\n                "threshold": threshold,\n                "qa": qa,\n                "kb": kb,\n                "kb_error": kb_error,\n                "graph": graph,\n                "graph_error": graph_error,\n            },\n            ensure_ascii=False, indent=2,\n        ))\n        return 3 if (graph_error or kb_error) else 0\n\n    # ---- 人读格式：结论行在最前 --------------------------------------------\n    if guards:\n        print(\n            f"[⚠️ 井控红线] 问题命中 {(\'、\'.join(guards))}：历史台账对井喷零覆盖，"\n            "**不得按措施推荐作答**。"\n        )\n    print(headline)\n    if detail_line:\n        print(detail_line)\n    print(f"[检索问题] {args.query}")\n    if forced_note:\n        print(forced_note)\n    if definitional:\n        print(\n            f"[提问类型] 定义题 → 问答对阈值临时提到 {threshold:.2f}"\n            "（台账记录的是事故处置，术语定义应由《钻井事故与复杂问题》/图谱回答）"\n        )\n\n    if qa is None:\n        print(f"[跳过问答对] --source {source}：本次没有查尼日尔历史记录，答案只来自《钻井事故与复杂问题》")\n    else:\n        if qa["nulls"]:\n            print(\n                f"[⚠️ 问答对未发布] 有 {len(qa[\'nulls\'])} 条召回的 answer 为 null —— "\n                "向量命中但回表取不到主问答，去平台把对应类别点「发布」（平台部署说明 §8.4）"\n            )\n\n        if qa["state"] == "hit":\n            primary = qa["hits"][0]\n            if qa["promoted_aggregate"]:\n                print("[排序调整] 问题未带井名，已把聚合条目（综合结论那一类）提为首选（单案例条目会抢命中）")\n            print(\n                f"[来源] 问答对（尼日尔历史记录）│ {describe(primary)} │ "\n                f"相似度 {primary[\'similarity\']:.4f} │ QA-ID {primary[\'id\']}"\n            )\n            print(f"\\n{RULE}\\n{(primary[\'answer\'] or \'\').strip()}\\n{RULE}")\n            if len(qa["hits"]) > 1:\n                print("\\n[同时命中的其他条目]（供你判断，用户没追问就别拼进答案）")\n                for hit in qa["hits"][1:]:\n                    print(f"  {hit[\'similarity\']:.4f} {describe(hit)} │ {preview(hit[\'answer\'])}")\n        elif qa["all"]:\n            print("[问答对召回明细]")\n            for hit in qa["all"][:3]:\n                if not (hit["answer"] or "").strip():\n                    flag = "答案为空"\n                elif hit["similarity"] >= threshold:\n                    flag = "≥阈值"\n                else:\n                    flag = "<阈值"\n                print(f"  {hit[\'similarity\']:.4f} [{flag}] {describe(hit)} │ {preview(hit[\'answer\'])}")\n        else:\n            print("[问答对] 向量库零召回")\n\n    if kb_error:\n        print(f"\\n[知识库错误] {kb_error}")\n    elif kb and kb["state"] == "hit":\n        print_kb(kb, args.query)\n    elif kb:\n        print(f"\\n[知识库] 零片段：阈值 {kb[\'threshold\']} 以上没有内容（离题片段一般只有 0.12–0.25）")\n\n    if graph_error:\n        print(f"\\n[图谱错误] {graph_error}")\n    elif graph:\n        print_graph(graph)\n\n    if qa and qa["state"] == "hit":\n        stage = "qa"\n    elif kb and kb["state"] == "hit":\n        stage = "kb"\n    elif graph and graph["state"] == "hit":\n        stage = "graph"\n    else:\n        stage = "none"\n\n    tail = (\n        f"\\n[RESULT] decision={decision} stage={stage} source={source} "\n        f"qa_state={qa[\'state\'] if qa else \'skipped\'} "\n        f"qa_top={fmt_sim(qa[\'top_similarity\']) if qa else \'—\'} "\n        f"threshold={f\'{threshold:.2f}\' if threshold is not None else \'—\'} "\n        f"guards={\',\'.join(guards) if guards else \'none\'}"\n    )\n    if kb is not None or kb_error:\n        tail += f" kb={\'error\' if kb_error else kb[\'state\']}"\n    if graph is not None or graph_error:\n        tail += f" graph={\'error\' if graph_error else graph[\'state\']}"\n    print(tail)\n    return 3 if (graph_error or kb_error) else 0\n\n\nif __name__ == "__main__":\n    run_cli(main)\n',
      },
    ],
  },
]
